# Session Report Index

`.agents/SESSION_REPORT.md` remains the session-report entrypoint; read the indexed part files for the full report history.

The historical content was split mechanically to keep individual files below the 700-line repository limit. Content order is preserved in the part files.

## Parts

- Part 01: [.agents/session_report_parts/session_report_part_01.md](session_report_parts/session_report_part_01.md) (326 lines)
- Part 02: [.agents/session_report_parts/session_report_part_02.md](session_report_parts/session_report_part_02.md) (352 lines)
- Part 03: [.agents/session_report_parts/session_report_part_03.md](session_report_parts/session_report_part_03.md) (376 lines)
- Part 04: [.agents/session_report_parts/session_report_part_04.md](session_report_parts/session_report_part_04.md) (335 lines)
- Part 05: [.agents/session_report_parts/session_report_part_05.md](session_report_parts/session_report_part_05.md) (376 lines)
- Part 06: [.agents/session_report_parts/session_report_part_06.md](session_report_parts/session_report_part_06.md) (317 lines)
- Part 07: [.agents/session_report_parts/session_report_part_07.md](session_report_parts/session_report_part_07.md) (317 lines)
- Part 08: [.agents/session_report_parts/session_report_part_08.md](session_report_parts/session_report_part_08.md) (333 lines)
- Part 09: [.agents/session_report_parts/session_report_part_09.md](session_report_parts/session_report_part_09.md) (331 lines)
- Part 10: [.agents/session_report_parts/session_report_part_10.md](session_report_parts/session_report_part_10.md) (342 lines)
- Part 11: [.agents/session_report_parts/session_report_part_11.md](session_report_parts/session_report_part_11.md) (329 lines)
- Part 12: [.agents/session_report_parts/session_report_part_12.md](session_report_parts/session_report_part_12.md) (347 lines)
- Part 13: [.agents/session_report_parts/session_report_part_13.md](session_report_parts/session_report_part_13.md) (320 lines)
- Part 14: [.agents/session_report_parts/session_report_part_14.md](session_report_parts/session_report_part_14.md) (324 lines)
- Part 15: [.agents/session_report_parts/session_report_part_15.md](session_report_parts/session_report_part_15.md) (387 lines)
- Part 16: [.agents/session_report_parts/session_report_part_16.md](session_report_parts/session_report_part_16.md) (365 lines)
- Part 17: [.agents/session_report_parts/session_report_part_17.md](session_report_parts/session_report_part_17.md) (321 lines)
- Part 18: [.agents/session_report_parts/session_report_part_18.md](session_report_parts/session_report_part_18.md) (357 lines)
- Part 19: [.agents/session_report_parts/session_report_part_19.md](session_report_parts/session_report_part_19.md) (329 lines)
- Part 20: [.agents/session_report_parts/session_report_part_20.md](session_report_parts/session_report_part_20.md) (339 lines)
- Part 21: [.agents/session_report_parts/session_report_part_21.md](session_report_parts/session_report_part_21.md) (339 lines)
- Part 22: [.agents/session_report_parts/session_report_part_22.md](session_report_parts/session_report_part_22.md) (384 lines)
- Part 23: [.agents/session_report_parts/session_report_part_23.md](session_report_parts/session_report_part_23.md) (335 lines)
- Part 24: [.agents/session_report_parts/session_report_part_24.md](session_report_parts/session_report_part_24.md) (320 lines)
- Part 25: [.agents/session_report_parts/session_report_part_25.md](session_report_parts/session_report_part_25.md) (357 lines)
- Part 26: [.agents/session_report_parts/session_report_part_26.md](session_report_parts/session_report_part_26.md) (356 lines)
- Part 27: [.agents/session_report_parts/session_report_part_27.md](session_report_parts/session_report_part_27.md) (370 lines)
- Part 28: [.agents/session_report_parts/session_report_part_28.md](session_report_parts/session_report_part_28.md) (618 lines)
- Part 29: [.agents/session_report_parts/session_report_part_29.md](session_report_parts/session_report_part_29.md) (665 lines)
- Part 30: [.agents/session_report_parts/session_report_part_30.md](session_report_parts/session_report_part_30.md) (44 lines)
- Part 31: [.agents/session_report_parts/session_report_part_31.md](session_report_parts/session_report_part_31.md) (35 lines)
- Part 32: [.agents/session_report_parts/session_report_part_32.md](session_report_parts/session_report_part_32.md) (36 lines)
- Part 33: [.agents/session_report_parts/session_report_part_33.md](session_report_parts/session_report_part_33.md) (40 lines)
- Part 34: [.agents/session_report_parts/session_report_part_34.md](session_report_parts/session_report_part_34.md) (60 lines)
- Part 35: [.agents/session_report_parts/session_report_part_35.md](session_report_parts/session_report_part_35.md) (673 lines)
- Part 36: [.agents/session_report_parts/session_report_part_36.md](session_report_parts/session_report_part_36.md) (62 lines)
- Part 37: [.agents/session_report_parts/session_report_part_37.md](session_report_parts/session_report_part_37.md) (64 lines)
- Part 38: [.agents/session_report_parts/session_report_part_38.md](session_report_parts/session_report_part_38.md) (57 lines)
- Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md) (849 lines)

## 2026-04-19 21:42 CEST - All Eligible Over-700 Files Split

- Objective attempted:
  - Continue the split/audit pass beyond active source into project-owned
    documentation and operational history, while excluding generated artifacts,
    binaries, backup snapshots, disabled source, and tool state.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - documentation, memory history, TODO, agent plan/report, and generated-file
    classification.
- Code or configuration changes made:
  - Split `memory/CHANGELOG.md`, `TODO.md`, `.agents/PLAN.md`, and
    `.agents/SESSION_REPORT.md` into concise canonical index stubs plus
    nearby part files under 700 lines.
  - Used subagents to split `docs/SESSION_REPORT.md`,
    `memory/archive/CHANGELOG_ARCHIVE_2026-03-08.md`,
    `docs/LANGUAGE_SPEC.md`, `docs/deduce-datalog-spec.md`,
    `docs/PROJECT_TOOLING.md`, `docs/reference/08-libraries.md`,
    `docs/reference/03-collections.md`, and
    `tooling/omni-nvim/README.md`.
  - Split remaining eligible large docs under `docs/plans`, `docs/areas`,
    `memory`, and `report` into indexed part files.
  - Fixed generated index links so Markdown links resolve relative to each
    stub file instead of accidentally pointing through duplicated path
    prefixes.
- Commands run:
  - Fast worker subagents for historical docs and language/reference docs.
  - Fast explorer subagent for generated/artifact classification.
  - Whole-repo `wc -l` inventories excluding `.git`, `.jj`, `build`, `obj`,
    vendored dependencies, codegraph/swarm state, and `__pycache__`.
  - Index-link existence check for generated Markdown stubs.
  - `git diff --check`.
- Key results:
  - No eligible project-owned, non-artifact file remains above 700 LOC.
  - Superseded checkpoint: at this point remaining over-700 rows were treated
    as classified exclusions. Later raw-tracked-text closure split those text
    rows too, and the current inventory is zero tracked text files above 700
    LOC.
  - All generated Markdown index links checked by the audit script resolve.
- Invalidated assumptions or failed approaches worth preserving:
  - A split that leaves 3000-line archive parts is not a split. Historical
    archives need the same cap as source/docs when the user asks for all
    eligible files.
  - Repo-root-looking relative links inside nested stubs are broken when opened
    from the stub file. Part links must be relative to the stub directory.
- Current best recommendation/checkpoint:
  - Treat the file-splitting portion as closed for eligible project-owned
    files. Future split sweeps should start by applying the same exclusion
    classification instead of manually editing generated shader/parser outputs.
- Unresolved issues:
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
  - Generated artifacts remain above 700 by design; if the owner wants those
    below 700 too, the correct path is generator changes or regeneration, not
    hand-splitting blobs.
- Signature: Codex GPT-5.4

## 2026-04-19 21:52 CEST - Split Hygiene Follow-Up Audit

- Objective attempted:
  - Re-run the over-700 split gate and review the mechanical split output like
    a strict reviewer, then fix concrete issues.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - generated Markdown part files, LSP smoke split, C/C3 helper wiring, and
    remaining over-700 classification.
- Code or configuration changes made:
  - Removed trailing whitespace and extra blank EOFs introduced by mechanical
    Markdown splits.
  - No generated shader/parser blobs, vendored files, binaries, backup files,
    or disabled source snapshots were manually split.
- Commands run:
  - Fast explorer subagent for split fallout across docs, links, LSP smoke, and
    Python cache churn.
  - Fast explorer subagent for C/C3 helper build wiring and moved Tensor
    symbol ownership.
  - Whole-repo and maintainable-source/doc LOC inventories.
  - Markdown index-link existence audit.
  - `git diff --check`.
  - `PYTHONDONTWRITEBYTECODE=1 python3 tooling/omni-lsp/tests/smoke_test.py`.
  - Python syntax compile for all `tooling/omni-lsp` Python files.
  - `scripts/build_omni_chelpers.sh`.
  - `c3c build --obj-out obj`.
- Key results:
  - Superseded checkpoint: maintainable source/doc LOC gate printed no rows
    over 700 while the broad whole-repo inventory still reported classified
    exclusions. Later raw-tracked-text closure split those text rows too.
  - Markdown index links resolve.
  - LSP smoke passes and no Python bytecode churn is present.
  - C/C3 wiring audit found no missing helper build entries, no PTX include
    mismatch, and no duplicate moved Tensor ownership definitions.
  - `c3c build --obj-out obj` linked `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - Mechanical Markdown part generation can preserve unwanted trailing
    whitespace from old docs; always run `git diff --check` after split passes.
- Current best recommendation/checkpoint:
  - The split request is closed for eligible project-owned files. Remaining
    work should be commit hygiene and any broader full-suite validation the
    owner wants before pushing.
- Unresolved issues:
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4

## 2026-04-19 23:00 CEST - Missing Dependency Install and Docs Verification

- Objective attempted:
  - Install dependencies that blocked the prior split verification and prove
    the previously blocked checks can now run.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - Host Ubuntu/aarch64 package environment and FTXUI docs/test verification.
- Code or configuration changes made:
  - Installed `doxygen`, `graphviz`, `libgtest-dev`, and `googletest` via apt.
  - Fixed FTXUI's split Doxyfile flow so CMake configures the included
    Doxyfile fragments into the build directory before Doxygen reads them.
- Commands run:
  - `sudo apt-get update`
  - `sudo apt-get install -y doxygen graphviz libgtest-dev googletest`
  - `doxygen --version`, `dot -V`, and `/usr/include/gtest/gtest.h` checks.
  - `c++ -std=c++17 -Ithird_party/ftxui/include -Ithird_party/ftxui/src -fsyntax-only third_party/ftxui/src/ftxui/component/input_test.cpp`
  - `c++ -std=c++17 -Ithird_party/ftxui/include -Ithird_party/ftxui/src -fsyntax-only third_party/ftxui/src/ftxui/dom/table_test.cpp`
  - `cmake -S third_party/ftxui -B /tmp/omni-ftxui-docs-verify -DFTXUI_BUILD_DOCS=ON -DFTXUI_BUILD_EXAMPLES=OFF -DFTXUI_BUILD_TESTS=OFF -DFTXUI_ENABLE_INSTALL=OFF`
  - `cmake --build /tmp/omni-ftxui-docs-verify --target doc`
- Key results:
  - Doxygen 1.9.8, Graphviz `dot`, and GoogleTest headers are installed.
  - The previously blocked FTXUI syntax checks now pass.
  - The FTXUI Doxygen target now builds successfully from `/tmp`.
- Invalidated assumptions or failed approaches worth preserving:
  - Installing Doxygen was not sufficient by itself; split Doxyfile fragments
    must be configured before inclusion or raw CMake placeholders reach
    Doxygen.
- Current best recommendation/checkpoint:
  - Treat the prior missing-dependency validation blockers as resolved.
  - Remaining Doxygen output is warning-only upstream documentation noise, not
    a missing dependency or split wrapper failure.
- Unresolved issues:
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not rerun in this
    dependency-install pass.
- Signature: Codex GPT-5.4

## 2026-04-19 23:23 CEST - Vulkan ML Suite Backlog Added

- Objective attempted:
  - Convert the owner request for a complete Vulkan-backed ML suite into live,
    concrete backlog items.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - Vulkan/Tensor roadmap, TODO queue, and operational plan.
- Code or configuration changes made:
  - Added `docs/plans/vulkan-ml-suite-roadmap-2026-04-19.md`.
  - Added live `ML-VK-*` TODO lanes for capability contracts, batched linear
    algebra, neural kernels, convolution/pooling, normalization/attention,
    autograd, optimizers, model/layer serialization, graph execution, and
    validation/benchmarks.
  - Updated `docs/plans/README.md` and `.agents/PLAN.md` so the ML suite plan
    is discoverable.
- Key results:
  - The next Vulkan direction is now explicitly an ML suite, not only isolated
    matrix-kernel expansion.
  - The plan preserves the backend-neutral Tensor contract: explicit placement,
    capability reporting, no backend-named public APIs, and no hidden CPU
    fallback for Vulkan operands.
- Current best recommendation/checkpoint:
  - Start implementation with `ML-VK-001` to freeze public ML operation names
    and `tensor-backends` capability keys before adding ML kernels.
- Unresolved issues:
  - No implementation or runtime validation was run for ML kernels in this
    planning-only pass.
- Signature: Codex GPT-5.4

## 2026-04-19 23:41 CEST - Vulkan ML Contract Audit Slice

- Objective attempted:
  - Audit the Vulkan ML suite readiness, add concrete TODO checkboxes for
    findings, and implement the first feasible TODO items using subagent
    review support.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `tensor-backends`, Vulkan/Tensor docs, advanced collections regressions,
    and `ML-VK-001`.
- Code or configuration changes made:
  - Added explicit backend-neutral ML capability keys to every
    `tensor-backends` entry: `ml-linear`, `ml-convolution`, `ml-neural-map`,
    `ml-normalization`, `ml-attention`, `ml-autograd`, `ml-optimizer`, and
    `ml-graph-execution`. All report `false` until real backend kernels and
    fail-closed tests exist.
  - Added `stats-normal-float64` and `stats-normal-float32` capability keys so
    Vulkan Float64 `stats/normal-*` support is discoverable without widening
    the broad `scientific-map-float64` contract.
  - Added TODO audit checkboxes under `ML-VK-001`; completed the capability key
    and `stats-normal-*` discoverability items, leaving the first public ML
    fail-closed regression open.
- Commands run:
  - Subagent audit for ML-VK-001 capability/reporting gaps.
  - Subagent review for the narrow `stats-normal-*` capability direction.
  - `git diff --check`
  - `scripts/check_file_size_gate.sh`
  - `c3c build`
  - `LD_LIBRARY_PATH=/usr/local/lib OMNI_LISP_TEST_SLICE=advanced OMNI_ADVANCED_GROUP_FILTER=advanced-collections-module OMNI_TEST_SUMMARY=1 ./build/main --test-suite lisp`
- Key results:
  - Targeted advanced collections validation passed with `pass=1600 fail=0`.
  - File-size gate passed with no tracked text files above 700 LOC.
  - `c3c build` linked `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - Do not set Vulkan `scientific-map-float64` true just because
    `stats/normal-cdf` and `stats/normal-quantile` work; that key implies the
    broader scientific unary family.
- Current best recommendation/checkpoint:
  - Continue `ML-VK-001` with public ML operation-name freeze and fail-closed
    Vulkan tests before claiming or implementing any ML kernel family.
- Unresolved issues:
  - No full bounded-container suite was run.
  - `AUDIT-ML-VK-001-FAIL-CLOSED` remains open because the public unsupported
    ML operation names still need to be frozen.
- Signature: Codex GPT-5.4

## 2026-04-19 23:59 CEST - Vulkan ML First Operation Slice

- Objective attempted:
  - Continue the strict ML-VK-001 audit by freezing the first public ML
    operation name and implementing the first TODO item with subagent review.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `ml/linear`, `tensor-backends`, AOT primitive lookup, advanced collections
    tests, and ML roadmap/TODO state.
- Code or configuration changes made:
  - Added `src/lisp/prim_ml_linear.c3`.
  - Registered `ml/linear` in runtime primitive tables and AOT primitive lookup.
  - Implemented CPU dense row-major `Float64`/`Float32` affine projection:
    `input[..., in_features]` by `weights[out_features, in_features]`, plus
    optional `bias[out_features]`, producing `input[..., out_features]`.
  - Used `tensor_expr_resolve_concrete_cpu` so any non-CPU Tensor expression
    fails before realization. This preserves the no-hidden-CPU-fallback Vulkan
    contract.
  - Updated `tensor-backends` so `ml-linear` is true for CPU and false for
    Vulkan/CUDA/cuBLAS.
  - Marked the ML-VK-001 naming and fail-closed audit checkboxes complete.
- Commands run:
  - Subagent audit of primitive registration and Tensor fail-closed surfaces.
  - Subagent TODO wording audit for ML-VK-001 naming/fail-closed gaps.
  - `c3c build`
  - `scripts/check_file_size_gate.sh`
  - `git diff --check`
  - `scripts/check_primitive_docs_parity.sh`
  - `LD_LIBRARY_PATH=/usr/local/lib OMNI_LISP_TEST_SLICE=advanced OMNI_ADVANCED_GROUP_FILTER=advanced-collections-module OMNI_TEST_SUMMARY=1 ./build/main --test-suite lisp`
- Key results:
  - Focused advanced collections suite passed with `pass=1606 fail=0`.
  - File-size gate passed with no tracked text files above 700 LOC.
  - `c3c build` linked `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - Do not use `tensor_expr_resolve_concrete_any_device` for CPU-only ML
    primitives; it can realize device expressions before the primitive has
    made the explicit backend decision.
- Current best recommendation/checkpoint:
  - Start `ML-VK-010` by deciding whether Vulkan `ml/linear` should lower
    through the existing `contract` kernels first or receive a dedicated
    batched GEMM helper.
- Unresolved issues:
  - Full bounded-container suite was not run.
  - Vulkan `ml-linear` remains intentionally unsupported and capability-gated
    false.
- Signature: Codex GPT-5.4

## 2026-04-20 00:04 CEST - ML Linear Audit Hardening

- Objective attempted:
  - Continue the strict audit after CPU `ml/linear`, add TODO checkboxes for
    found gaps, and implement the next safe TODO slice using subagent review.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `ml/linear` tests, ML-VK-001 TODO closure, and ML-VK-010 backlog shape.
- Code or configuration changes made:
  - Added audit TODO checkboxes for `ml/linear` non-CPU operand coverage and
    CPU oracle shape coverage, then implemented both through tests.
  - Added focused tests for lazy CPU input realization, rank-3 batch
    projection, dtype mismatch diagnostics, Vulkan weights fail-closed, and
    Vulkan bias fail-closed.
  - Split `ML-VK-010` into executable subitems for first Vulkan `Float32`
    `ml/linear` lowering, batched matmul coverage, and bias-add/reduction
    coverage.
- Commands run:
  - Two subagent audits of `ml/linear` correctness and ML-VK TODO shape.
  - `c3c build`
  - `scripts/check_file_size_gate.sh`
  - `git diff --check`
  - `LD_LIBRARY_PATH=/usr/local/lib OMNI_LISP_TEST_SLICE=advanced OMNI_ADVANCED_GROUP_FILTER=advanced-collections-module OMNI_TEST_SUMMARY=1 ./build/main --test-suite lisp`
- Key results:
  - Focused advanced collections suite passed with `pass=1611 fail=0`.
  - File-size gate passed with no tracked text files above 700 LOC.
  - `c3c build` linked `build/main`.
- Current best recommendation/checkpoint:
  - Implement `ML-VK-010-001` next. Do not flip Vulkan `ml-linear` true until
    placement-preserving Vulkan execution and mixed-device/layout rejection are
    both tested.
- Unresolved issues:
  - Full bounded-container suite was not run.
  - Vulkan `ml-linear` remains intentionally unsupported and capability-gated
    false.
- Signature: Codex GPT-5.4

## 2026-04-19 22:34 CEST - Generated SPIR-V Split Follow-Up

- Objective attempted:
  - Continue the >700 LOC split pass beyond maintainable handwritten files and
    remove the remaining oversized generated Vulkan SPIR-V C wrappers.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `csrc/*_spv.c`, new `csrc/*_spv_part_*.inc` chunk files, and
    `scripts/check_file_size_gate.sh`.
- Code or configuration changes made:
  - Split oversized generated Vulkan SPIR-V C array wrappers into small wrapper
    files plus ordered under-700 `.inc` chunks included inside the initializer.
  - Tightened `scripts/check_file_size_gate.sh` so generated Vulkan `_spv.c`
    wrappers are no longer exempt. Generated PTX and tree-sitter parser output
    remain excluded because they still require generator-aware handling.
- Commands run:
  - Fast worker subagent for generated SPIR-V wrapper splitting.
  - Fast explorer subagent for SPIR-V build wiring audit.
  - Fast explorer subagent for vendor/tree-sitter ownership audit.
  - Raw jj-tracked text LOC inventory.
  - `scripts/check_file_size_gate.sh`.
  - `bash -n scripts/check_file_size_gate.sh scripts/run_global_gates.sh`.
  - `git diff --check`.
  - `scripts/build_omni_chelpers.sh`.
  - `c3c build --obj-out obj`.
- Key results:
  - All `csrc/*_spv.c` files are now at or below 695 LOC.
  - Largest `csrc/*_spv_part_*.inc` chunk is 550 LOC.
  - Raw jj-tracked text inventory now reports 11 over-700 files: all
    `third_party/*` vendor payloads or generated
    `tooling/tree-sitter-omni/src/parser.c`.
  - File-size gate passes with `_spv.c` files checked.
  - Helper archive build passed and `c3c build --obj-out obj` linked
    `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - Generated SPIR-V C wrappers should not remain exempt after the repository
    has an established wrapper-plus-chunks pattern. Future oversized `_spv.c`
    regeneration should fail the gate until it is chunked again.
- Current best recommendation/checkpoint:
  - Treat the only remaining >700 files as explicit non-split ownership
    exclusions: vendored `third_party/*` and generated tree-sitter parser
    output.
- Unresolved issues:
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4

- 2026-04-19 validated mechanical split of oversized generated Vulkan SPIR-V C
  blobs in `/home/christos/Omni`.
  - Objective: split every `csrc/*_spv.c` over 700 LOC into a small wrapper
    plus ordered under-700 `*_spv_part_*.inc` chunks, without touching
    `third_party`, tree-sitter, `project.json`, or build scripts.
  - Changed files: the oversized SPIR-V wrappers now include chunked
    `.inc` payloads; new `.inc` parts were created alongside them. No other
    source or build files were edited in this slice.
  - Commands run: raw `wc -l` inventories over `csrc/*_spv.c` and
    `csrc/*_spv_part_*.inc`; `./scripts/build_omni_chelpers.sh`; `jj status`.
  - Key results: all `csrc/*_spv.c` files now sit at or below 695 lines; the
    largest generated chunk file is 550 lines; helper build passed cleanly.
  - Invalidated assumption: the previous generated SPIR-V C blobs were not
    safe to leave as monolithic wrappers once they crossed the repo line cap;
    they now behave like the existing chunked generated assets in this tree.
  - Current best recommendation: keep future SPIR-V regeneration on the same
    wrapper-plus-chunks pattern and re-run the raw LOC inventory after any
    shader regeneration.
  - Unresolved issues: none for this slice.
  - Signature: Codex GPT-5.4

## 2026-04-19 22:07 CEST - File Size Gate And Artifact Cleanup

- Objective attempted:
  - Turn the repeated over-700 LOC split rule into an enforceable gate and
    clean obvious tracked artifacts found by the audit.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `scripts/check_file_size_gate.sh`, `scripts/run_global_gates.sh`,
    `.gitignore`, `.codegraph` logs, Python bytecode caches, and tracked
    over-700 inventory.
- Code or configuration changes made:
  - Added `scripts/check_file_size_gate.sh`. It fails tracked maintainable
    text files above 700 LOC and explicitly excludes vendor/dependency code,
    generated PTX/parser output, repo-state artifacts, backup/disabled
    snapshots, Python bytecode caches, and non-text binaries. Generated Vulkan
    `_spv.c` wrappers are now checked after this continuation split.
  - Wired the file-size gate into `scripts/run_global_gates.sh` as Stage 0.
  - Updated the gate to use `jj file list` when available, with Git as the
    fallback, so newly added jj-tracked files are included before Git export.
  - Added `*.pyc`, `__pycache__/`, and `*/__pycache__/` to `.gitignore`.
  - Untracked generated `.codegraph/logs/*`, `.codegraph/daemon.pid`, and
    tracked Python bytecode cache files.
  - Split `plan.jsonl` into a one-line JSONL index plus ordered
    `plan_jsonl_parts/*.jsonl` shards. All shards parse as JSONL, stay under
    700 LOC, and stay below the jj new-file size cap.
  - Untracked and ignored `test_aot` as a top-level ELF test binary.
- Commands run:
  - Fast explorer subagent for tracked artifact classification.
  - Fast explorer subagent for `third_party` vendor classification.
  - `jj file untrack` for generated `.codegraph` logs/daemon pid and Python
    bytecode caches.
  - `scripts/check_file_size_gate.sh`.
  - `bash -n scripts/check_file_size_gate.sh scripts/run_global_gates.sh`.
  - `git diff --check`.
  - Markdown index-link audit.
  - `PYTHONDONTWRITEBYTECODE=1 python3 tooling/omni-lsp/tests/smoke_test.py`.
  - Python syntax compile for `tooling/omni-lsp` files.
  - `scripts/build_omni_chelpers.sh`.
  - `c3c build --obj-out obj`.
- Key results:
  - Superseded checkpoint: file-size gate passed while the raw tracked-text
    inventory still reported 11 over-700 vendor/generated text files. Later
    raw-tracked-text closure split those rows too and removed the
    vendor/generated text exemptions from the gate.
  - `plan.jsonl` no longer needs a size-gate exemption; its index and shards
    pass the same tracked-file gate as other maintainable artifacts.
  - `test_aot` is no longer tracked; the broad Git inventory previously showed
    it as the largest tracked row, but it is a binary artifact.
  - No Python bytecode or `.codegraph/logs` files remain tracked by `jj file
    list`.
  - `c3c build --obj-out obj` linked `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - A clean source/doc split gate is not enough if tracked artifacts remain in
    the repo. Bytecode and generated logs need ignore/untrack cleanup, not
    splitting.
- Current best recommendation/checkpoint:
  - Treat over-700 enforcement as automated through
    `scripts/check_file_size_gate.sh`; use that script for future reviews
    instead of ad hoc inventory commands.
- Unresolved issues:
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4


## 2026-04-19 22:15 CEST - Remaining Vendor Over-700 Split

- Objective attempted:
  - Split the remaining oversized vendor C/C++/header files into wrapper files
    plus ordered under-700 `.inc` chunks, preserving code content and keeping
    the original paths as thin include wrappers.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - `third_party/tomlc17/tomlc17.c`
  - `tooling/tree-sitter-omni/src/parser.c`
  - `third_party/ftxui/src/ftxui/screen/string.cpp`
  - `third_party/ftxui/src/ftxui/component/screen_interactive.cpp`
  - `third_party/ftxui/src/ftxui/dom/canvas.cpp`
  - `third_party/arena/vmem_arena.h`
  - `third_party/ftxui/src/ftxui/component/input_test.cpp`
  - `third_party/ftxui/src/ftxui/dom/table_test.cpp`
  - `third_party/ftxui/src/ftxui/component/menu.cpp`
- Code or configuration changes made:
  - Replaced each target file body with top-level `#include` lines pointing to
    ordered chunk files.
  - Created adjacent `.inc` shards for each file, each under 700 LOC.
  - After a first pass that cut `tomlc17.c` inside a block comment, regenerated
    the shards on safer boundaries before finishing the split.
- Commands run:
  - Raw `wc -l` inventory for the nine target paths and generated chunk files.
  - `scripts/build_omni_chelpers.sh`.
  - `git diff --check --` on the touched paths.
- Key results:
  - All nine wrapper files are now tiny include-only shims.
  - All generated `.inc` chunks are under 700 LOC.
  - `scripts/build_omni_chelpers.sh` passed after the safe-boundary resplit.
  - `git diff --check` passed for the touched paths.
- Invalidated assumptions or failed approaches worth preserving:
  - A raw 700-line split is not safe for C/C++ sources with block comments or
    other preprocessor-sensitive spans.
- Current best recommendation/checkpoint:
  - Treat the remaining over-700 vendor file list as closed for the requested
    paths. The workspace still contains many unrelated pre-existing changes in
    other files; do not infer they belong to this slice.
- Unresolved issues:
  - None for this task slice.
- Signature: Codex GPT-5.4

## 2026-04-19 22:48 CEST - Raw Tracked LOC Closure

- Objective attempted:
  - Apply the repeated owner instruction literally: no jj-tracked text file
    should remain above 700 LOC, including vendored and generated text.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - remaining over-700 `third_party/*` files, generated tree-sitter parser
    output, FTXUI documentation/config, and `scripts/check_file_size_gate.sh`.
- Code or configuration changes made:
  - Split the remaining oversized vendor C/C++/header files and
    `tooling/tree-sitter-omni/src/parser.c` into include-only wrappers plus
    adjacent ordered `.inc` chunks under 700 LOC.
  - Split `third_party/ftxui/doc/Doxyfile.in` into a configured stub plus
    ordered `Doxyfile.in.parts/*.txt` fragments. Fixed the stub to use
    `@CMAKE_CURRENT_SOURCE_DIR@/...` so CMake-configured Doxygen files include
    fragments from the source tree, not the build directory.
  - Split `third_party/ftxui/doc/mainpage.md` into an index stub plus ordered
    markdown part files. Removed the duplicate `\mainpage` marker from the
    stub so the real main page owner remains in part 01.
  - Removed the file-size gate exemptions for third-party, generated SPIR-V,
    generated PTX, and generated tree-sitter text files.
  - Cleaned mechanical split whitespace and extra blank EOFs flagged by
    `git diff --check`.
- Commands run:
  - Fast worker subagent for C/C++/header/tree-sitter include-wrapper splits.
  - Fast worker subagent for FTXUI doc/config split.
  - Raw jj-tracked text LOC inventory with no ownership exclusions.
  - `scripts/check_file_size_gate.sh`.
  - `bash -n scripts/check_file_size_gate.sh scripts/run_global_gates.sh`.
  - `git diff --check`.
  - `cc -Itooling/tree-sitter-omni/src -Itooling/tree-sitter-omni/src/tree_sitter -fsyntax-only tooling/tree-sitter-omni/src/parser.c`.
  - `scripts/build_omni_chelpers.sh`.
  - `c3c build --obj-out obj`.
- Key results:
  - Raw jj-tracked text inventory reports `total_over_700=0`.
  - Largest tracked text file is exactly 700 lines:
    `tooling/tree-sitter-omni/src/parser_part_3.c.inc`.
  - File-size gate passes with no vendor/generated source exclusions.
  - Tree-sitter generated parser wrapper passes C syntax.
  - Helper archive build passed and `c3c build --obj-out obj` linked
    `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - The previous policy-exclusion closure was weaker than the repeated owner
    request. The current gate now enforces the raw tracked text cap instead of
    relying on ownership exclusions for vendored/generated text.
  - Doxygen `@INCLUDE` paths in a configured `Doxyfile.in` must be source-dir
    anchored; relative fragment paths can break once CMake writes `Doxyfile`
    into the build directory.
- Unresolved issues:
  - `doxygen` is unavailable locally, so the FTXUI Doxygen output was not
    runtime-rendered.
  - FTXUI `input_test.cpp` and `table_test.cpp` syntax-only validation is
    blocked by missing `gtest/gtest.h` in this workspace.
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4

## 2026-04-19 23:08 CEST - Bad-Mood Split Audit Fixes

- Objective attempted:
  - Re-audit the raw LOC closure like a strict reviewer and fix concrete
    split regressions instead of accepting mechanical chunking at face value.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - include-wrapper chunks, FTXUI doc/config stubs, LSP/Neovim split surfaces,
    helper build manifests, and the file-size gate.
- Code or configuration changes made:
  - Fixed `third_party/arena/vmem_arena.h` split wiring. The prior split left
    `VMEM_ARENA_IMPLEMENTATION` conditionals open across sibling include files,
    which breaks standalone preprocessing. The wrapper now owns the continuation
    include guard and both declaration-only and implementation-enabled syntax
    checks pass.
  - Deleted the newly added dead stub `csrc/tensor_vulkan_helpers_matrix_ops.c`;
    it was not in `project.json` or `scripts/build_omni_chelpers.sh` and only
    duplicated the split-child file list.
  - Updated `.agents/PLAN.md` wording so the current checkpoint says raw
    tracked text is under the cap instead of relying on older
    vendor/generated-exclusion language.
- Commands run:
  - Fast explorer audit for C/C++/header/tree-sitter include-wrapper splits.
  - Fast explorer audit for FTXUI doc/config and local markdown indexes.
  - Fast explorer audit for LSP/Neovim/runtime split surfaces.
  - Raw jj-tracked text LOC inventory.
  - Include target existence and chunk line-count audits.
  - Coarse block-comment boundary audit for C/C++/header chunks.
  - `scripts/check_file_size_gate.sh`.
  - `git diff --check`.
  - `scripts/build_omni_chelpers.sh`.
  - `cc -Ithird_party/arena -fsyntax-only third_party/arena/vmem_arena.h`.
  - `cc -DVMEM_ARENA_IMPLEMENTATION -Ithird_party/arena -fsyntax-only third_party/arena/vmem_arena.h`.
  - `cc -Itooling/tree-sitter-omni/src -Itooling/tree-sitter-omni/src/tree_sitter -fsyntax-only tooling/tree-sitter-omni/src/parser.c`.
  - `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tooling/omni-lsp/*.py tooling/omni-lsp/tests/*.py`.
  - `PYTHONDONTWRITEBYTECODE=1 python3 tooling/omni-lsp/tests/smoke_test.py`.
  - `nvim --headless -u NONE ... require("omni").setup({lsp={enable=false}})`.
  - `c3c build --obj-out obj`.
- Key results:
  - Raw jj-tracked text inventory still reports `total_over_700=0`.
  - Largest tracked text file remains exactly 700 lines:
    `tooling/tree-sitter-omni/src/parser_part_3.c.inc`.
  - File-size gate passes and reports no tracked text files above 700 LOC.
  - Arena header split is repaired for both declaration-only and implementation
    modes.
  - Helper archive build and C3 integration build both pass; `c3c build
    --obj-out obj` linked `build/main`.
- Invalidated assumptions or failed approaches worth preserving:
  - Include-wrapper splitting can still break headers even when line counts and
    aggregate builds look fine. Preprocessor conditionals must not be left open
    across sibling include files unless the parent wrapper deliberately owns
    that conditional structure.
- Unresolved issues:
  - `doxygen` is unavailable locally, so FTXUI documentation rendering remains
    unexecuted.
  - FTXUI gtest translation-unit checks remain blocked because
    `gtest/gtest.h` is absent in this workspace.
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4

## 2026-04-19 23:30 CEST - Split Index And Vulkan Cache Audit

- Objective attempted:
  - Continue the strict review after raw LOC closure, focusing on stale split
    metadata and obvious code defects left by mechanical extraction.
- Relevant workspace or target:
  - `/home/christos/Omni`
  - split index stubs, `csrc/tensor_vulkan_helpers_core_context.inc`, wrapper
    chunks, LSP/Neovim tooling, helper build manifests, and the file-size gate.
- Code or configuration changes made:
  - Refreshed declared line counts in split index stubs from the actual part
    files:
    `.agents/SESSION_REPORT.md`, `.agents/PLAN.md`, `TODO.md`,
    `memory/CHANGELOG.md`, and `memory/DESTINATION_ARENA_PLAN.md`.
  - Removed the duplicate `omni_tensor_vulkan_int64_cached = 0;` reset in
    `csrc/tensor_vulkan_helpers_core_context.inc`; the Vulkan probe cache reset
    now initializes available/float64/int64 exactly once.
  - Marked older session-report “classified exclusions” checkpoints as
    superseded by the later raw-tracked-text closure to reduce audit noise.
- Commands run:
  - Fast explorer audit for include-wrapper/generated chunks.
  - Fast explorer audit for split index stubs and JSONL.
  - Fast explorer audit for build/runtime/LSP/Neovim split surfaces.
  - Raw jj-tracked text LOC inventory.
  - Split index count-drift audit.
  - JSONL parse audit for `plan.jsonl` and all `plan_jsonl_parts`.
  - Stub relative-link audit.
  - Part include existence audit.
  - `scripts/check_file_size_gate.sh`.
  - `git diff --check`.
  - `scripts/build_omni_chelpers.sh`.
  - `cc -Ithird_party/arena -fsyntax-only third_party/arena/vmem_arena.h`.
  - `cc -DVMEM_ARENA_IMPLEMENTATION -Ithird_party/arena -fsyntax-only third_party/arena/vmem_arena.h`.
  - `cc -Itooling/tree-sitter-omni/src -Itooling/tree-sitter-omni/src/tree_sitter -fsyntax-only tooling/tree-sitter-omni/src/parser.c`.
  - `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tooling/omni-lsp/*.py tooling/omni-lsp/tests/*.py`.
  - `PYTHONDONTWRITEBYTECODE=1 python3 tooling/omni-lsp/tests/smoke_test.py`.
  - `c3c build --obj-out obj`.
- Key results:
  - Raw jj-tracked text inventory still reports `total_over_700=0`.
  - Largest tracked text file remains exactly 700 lines:
    `tooling/tree-sitter-omni/src/parser_part_3.c.inc`.
  - Split index count-drift audit reports `count_drift 0`.
  - File-size gate passes and `c3c build --obj-out obj` linked `build/main`.
  - JSONL parsing, stub link checks, include target checks, helper build, LSP
    compile/smoke, arena header checks, and tree-sitter parser syntax all pass.
- Invalidated assumptions or failed approaches worth preserving:
  - Split index line counts are part of the handoff contract. If part files are
    edited after the split, regenerate the counts immediately instead of
    leaving advisory numbers that future agents must distrust.
- Unresolved issues:
  - `doxygen` is unavailable locally, so FTXUI documentation rendering remains
    unexecuted.
  - FTXUI gtest translation-unit checks remain blocked because
    `gtest/gtest.h` is absent in this workspace.
  - Full bounded-container `OMNI_LISP_TEST_SLICE=all` was not run.
- Signature: Codex GPT-5.4

## 2026-04-20 23:28 CEST - Vulkan Clip-Gradients Float32 Runtime

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented Vulkan dense row-major `Float32` `ml/clip-gradients` and
    optimizer `clip-norm` support.
  - Added sum-squares and scale shaders/helpers, C3 dispatch, capability
    reporting through `ml-clip-gradients-float32`, tests, docs, TODO, roadmap,
    plan, and changelog updates.
  - Validation passed: shader compile/`spirv-val`, helper archive rebuild,
    `c3c build`, direct Vulkan smokes, focused advanced collections
    `pass=1811 fail=0`, basic Lisp `pass=160 fail=0`, compiler slice
    `pass=281 fail=0`, primitive docs parity, Stage 3 source parity, code
    file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-007 Checked Vulkan Single-Node Kernel Capture

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented `kernel/capture(kernel inputs push)` for checked Vulkan
    direct-helper `Float32` Kernel launch planning.
  - Capture validates runtime inputs/push data and returns a single-node
    `kernel-graph` with operation, family, dtype, device, shape, execution, node
    data, and invalidation key without launching the kernel.
  - Validation: `c3c build`, focused advanced collections `pass=1846 fail=0`,
    compiler slice `pass=288 fail=0`, basic Lisp `pass=161 fail=0`,
    primitive docs parity, Stage 3 source parity, code file-size gate, and
    `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-006 Checked Vulkan Unary Float32 Kernel Family

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented checked Vulkan unary `Float32` `kernel/run` operations:
    `abs-f32`, `neg-f32`, `sqrt-f32`, `identity-f32`, `zero-f32`, `sin-f32`,
    `cos-f32`, `tan-f32`, `asin-f32`, `acos-f32`, `atan-f32`, `sinh-f32`,
    `cosh-f32`, `tanh-f32`, `exp-f32`, `log-f32`, `log10-f32`, and
    `normal-cdf-f32`.
  - Split the unary runner into `src/lisp/prim_kernel_unary.c3` and added it
    to the AOT runtime source manifest.
  - Validation: `c3c build`, focused advanced collections `pass=1844 fail=0`,
    basic Lisp `pass=161 fail=0`, compiler slice `pass=287 fail=0`,
    primitive docs parity, Stage 3 source parity, code file-size gate, and
    `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-20 - CUDA Map-Backed Optimizer CUDA Scope

- Updated `memory/changelog_parts/changelog_part_36.md` and `.agents/session_report_parts/session_report_part_39.md`
  to record map-backed CUDA optimizer coverage for `adam`, `adamw`, and `rmsprop`.
- Added corresponding docs/planning/tests edits and kept `ml-optimizer` broad false
  while fused CUDA optimizer kernels remain intentionally deferred.
- `docs/LANGUAGE_SPEC.part-01b.md`, `docs/reference/03-collections.part-01.md`, and
  `docs/reference/11-appendix-primitives.md` were updated through `session_report_part_39`
  checkpoints and now reflect CUDA map-backed optimizer progress.
- Commands noted: `c3c build`, targeted advanced collections module slice,
  `git diff --check`, and `scripts/check_primitive_docs_parity.sh`.
- Signature: Codex GPT-5.4

## 2026-04-21 - ML-VK-070-005 Dense NN Training Facade

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented `nn/forward`, `nn/grad`, and `nn/train-step` over train-mode
    dense model data.
  - `nn/train-step` returns updated model data, optimizer state, gradients,
    loss, and output without hidden mutation.
  - CUDA/Vulkan backward remains fail-closed through the existing `ml/grad`
    contract.
  - Validation: `scripts/build_omni_chelpers.sh`, `c3c build`, focused
    advanced collections `pass=1828 fail=0`, compiler slice `pass=284 fail=0`,
    basic Lisp `pass=160 fail=0`, primitive docs parity, Stage 3 source parity,
    code file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-070-006 Optimizer Spec Constructors

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented `nn/sgd`, `nn/adam`, `nn/adamw`, and `nn/rmsprop`.
  - Constructors validate hyperparameters and return ordinary optimizer spec
    dictionaries for `nn/train-step` / `ml/optimizer-step`.
  - No hidden optimizer state or mutation was introduced.
  - Validation: `scripts/build_omni_chelpers.sh`, `c3c build`, direct eval
    smokes, focused advanced collections `pass=1831 fail=0`, compiler slice
    `pass=285 fail=0`, basic Lisp `pass=160 fail=0`, primitive docs parity,
    Stage 3 source parity, code file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-001 Kernel Value Surface

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented `Kernel(spec)` as a validated dictionary-backed custom kernel
    value with `type-of` / `is?` reporting `Kernel`.
  - Added explicit `kernel/run(kernel inputs push)`, currently fail-closed with
    `tensor/backend-unsupported` until backend compilation/launch exists.
  - Fixed postfix path/index chaining so `rows.[0].name` and
    `k.inputs.[0].name` parse while leading-dot accessors remain rejected.
  - Validation: `c3c build`, direct eval smokes, focused advanced collections
    exit 0, compiler slice `pass=287 fail=0`, basic Lisp `pass=161 fail=0`,
    primitive docs parity, Stage 3 source parity, code file-size gate, and
    `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-002 Checked Vulkan Kernel Runner

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented the first real `kernel/run` backend route:
    `backend 'vulkan`, `operation 'scale-f32`.
  - The runner validates one Float32 input/output descriptor pair, matching
    tensor shape, dense row-major Vulkan Float32 storage, and a Float32 `scale`
    push constant.
  - It returns an ordinary output dictionary and keeps unsupported compilation,
    graph capture, fusion, batching, and `(define [kernel] ...)` sugar out of
    scope.
  - Validation: `c3c build`, direct eval smokes, focused advanced collections
    `pass=1837 fail=0`, compiler slice `pass=287 fail=0`, basic Lisp
    `pass=161 fail=0`, primitive docs parity, Stage 3 source parity, code
    file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-008 Vulkan Tensor Map Graph Capture

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Implemented `tensor/capture(source)` for supported all-Vulkan `Float32`
    concrete/map Tensor expression graphs.
  - Explicit `to-device 'vulkan` now preserves supported CPU lazy `Float32` map
    graphs as Vulkan expressions, giving capture real source/map DAG data
    without launching the graph.
  - Capture returns ordinary `tensor-graph` metadata with source/map nodes,
    scalar operands, output id, shape, and invalidation key.
  - Validation: `c3c build`, direct capture smoke, focused advanced collections
    `pass=1848 fail=0`, compiler slice `pass=289 fail=0`, basic Lisp
    `pass=161 fail=0`, primitive docs parity, Stage 3 source parity, code
    file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-009 Vulkan Tensor Contract Graph Capture

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Extended `to-device 'vulkan` graph preservation to supported CPU lazy
    `Float32` contract expressions.
  - Extended `tensor/capture(source)` to return all-Vulkan `Float32`
    concrete/map/contract graph data with `tensor-contract` nodes recording
    input ids, left/right axes, axis count, output shape, dtype/device/backend,
    and invalidation metadata.
  - The graph plan remains non-executing and metadata-only; view capture,
    batching, fusion, source compilation, and buffer reuse remain open.
  - Validation: `c3c build`, direct contract-capture eval smoke, focused
    advanced collections `pass=1849 fail=0`, compiler slice `pass=289 fail=0`,
    basic Lisp `pass=161 fail=0`, primitive docs parity, Stage 3 source parity,
    code file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex

## 2026-04-21 - ML-VK-080-010 Vulkan Tensor Direct View Graph Capture

- Canonical report entry:
  - See Part 39: [.agents/session_report_parts/session_report_part_39.md](session_report_parts/session_report_part_39.md).
- Summary:
  - Extended `tensor/capture(source)` to return all-Vulkan `Float32`
    direct-transpose-view graph data.
  - Added `tensor-view` graph nodes for direct rank-2 `matrix/transpose-view`
    values over concrete dense Vulkan `Float32` storage, recording input id,
    output shape, strides, storage offset, dtype/device/backend, operation, and
    invalidation metadata.
  - Arbitrary strided view graphs, batching, fusion, source compilation, and
    buffer reuse remain open.
  - Validation: `c3c build`, direct view-capture eval smoke, focused advanced
    collections `pass=1850 fail=0`, compiler slice `pass=289 fail=0`, basic
    Lisp `pass=161 fail=0`, primitive docs parity, Stage 3 source parity, code
    file-size gate, and `git diff --check`.
- Signature: GPT-5 Codex
