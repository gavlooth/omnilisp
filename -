/* Generated by OmniLisp Compiler */
/* ASAP Memory Management - Compile-Time Free Injection */

#include "/home/heefoo/Documents/code/OmniLisp/csrc/../runtime/include/omni.h"

#define NIL mk_pair(NULL, NULL)
#define NOTHING mk_nothing()
#define omni_print(o) prim_print(o)
#define car(o) obj_car(o)

/* Runtime Type Objects */
extern Obj* o_Int;    /* Int type object */
extern Obj* o_String; /* String type object */
extern Obj* o_Any;    /* Any type object */
extern Obj* o_Nothing; /* Nothing type object */

#define cdr(o) obj_cdr(o)
#define mk_cell(a, b) mk_pair(a, b)
#define prim_cons(a, b) mk_pair(a, b)

int main(void) {
    /* Region-RC: Create global region for main() */
    struct Region* _local_region = region_create();
    
    /* Initialize type objects for dispatch */
    omni_init_type_objects();
    
    {
        Obj* _result = (prim_println(mk_pair(mk_int(1), mk_pair(mk_int(2), mk_pair(mk_int(3), NULL)))), NOTHING);
        omni_print(_result);
        printf("\n");
        free_obj(_result);
    }
    
    /* Region-RC: Cleanup global region */
    region_exit(_local_region);
    region_destroy_if_dead(_local_region);
    return 0;
}
